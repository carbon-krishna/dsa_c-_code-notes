// 🔗 C++ Set Algorithms (Header: <algorithm>)
// All set algorithms assume the input ranges are sorted.

// 🟢 std::set_union
// Purpose: Combines all elements from two sorted ranges, removing duplicates.

// Syntax:
// set_union(a.begin(), a.end(), b.begin(), b.end(), back_inserter(result));

// 🟣 std::set_intersection
// Purpose: Finds the common elements between two sorted ranges.

// Syntax:
// set_intersection(a.begin(), a.end(), b.begin(), b.end(), back_inserter(result));

// 🔻 std::set_difference
// Purpose: Finds elements present in the first range but not in the second.

// Syntax:
// set_difference(a.begin(), a.end(), b.begin(), b.end(), back_inserter(result));

// 🔁 std::set_symmetric_difference
// Purpose: Finds elements present in either of the two ranges but not in both.

// Syntax:
// set_symmetric_difference(a.begin(), a.end(), b.begin(), b.end(), back_inserter(result));
